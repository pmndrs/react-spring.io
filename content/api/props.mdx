---
title: Shared Props
---

The following props are shared across the entire API, with some exceptions.

# The `to` prop

# The `from` prop

# The `config` prop

# The `immediate` prop

# The `delay` prop

# The `reset` prop

# The `cancel` prop

# The `` prop

# The `` prop

# The `` prop

# The `` prop

# The `` prop
